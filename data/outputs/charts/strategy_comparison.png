import pandas as pd

# Load historical data
historical_data = pd.read_csv('data/inputs/historical_data.csv')
historical_data['Date'] = pd.to_datetime(historical_data['Date'])

# Simulate backtest
from src.backtesting import Backtesting
from src.visualization import Visualization
from src.performance_metrics import BacktestReport

# Strategy Configuration
strategy_config = {
    "lookback_period": 14,
    "rsi_buy_threshold": 30,
    "rsi_sell_threshold": 70
}
starting_cash = 100000
portfolio_value = Backtesting.run_backtest(historical_data, strategy_config, starting_cash)

# Generate outputs
BacktestReport.generate_report(historical_data, starting_cash)
Visualization.plot_equity_curve(historical_data)
Visualization.plot_drawdown(historical_data)
